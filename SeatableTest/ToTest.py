# 测试写入''后表内当前值
"""
写入后当前值为''
"""

# 测试字典内没有键时能否update
"""
如果实表中有该名称的列，则可以更新
如果实表中没有该名称的列，则更新无效
"""

#如果在列中写入不符合该列类型的值，结果
"""
如果原data中没有该key，则在data中增加key,value为None
并且在实表中删除不了
如果原data中有该key，则该key的value变为写入的值，但在实表中不显示
并且在实表中删除该单元格内容后，则该key的value又变回''
"""